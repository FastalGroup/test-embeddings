# =============================================================================
# Configurazione Vector Search
# =============================================================================
# Copia questo file in .env e inserisci i tuoi valori.
# Il file .env NON va committato in git (contiene segreti).
# =============================================================================

# --- OpenAI API ---
# Chiave API per OpenAI (obbligatoria)
# Ottienila da: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-...

# --- Modelli ---
# Modello per gli embeddings (opzionale, default: text-embedding-3-small)
# Opzioni: text-embedding-3-small, text-embedding-3-large, text-embedding-ada-002
EMBEDDING_MODEL=text-embedding-3-small

# Modello LLM per RAG (opzionale, default: gpt-4o-mini)
# Opzioni: gpt-4o-mini, gpt-4o, gpt-4-turbo
LLM_MODEL=gpt-4o-mini

# Temperatura LLM (opzionale, default: 0.7)
# Range: 0.0 (deterministico) - 2.0 (molto creativo)
LLM_TEMPERATURE=0.7

# --- Chunking ---
# Dimensione chunk in caratteri (opzionale, default: 1000)
CHUNK_SIZE=1000

# Sovrapposizione tra chunk (opzionale, default: 200)
CHUNK_OVERLAP=200

# --- RAG ---
# Numero documenti da recuperare per il contesto (opzionale, default: 4)
RAG_NUM_DOCS=4

# --- Storage ---
# Directory per il database Chroma (opzionale, default: ./chroma_db)
# CHROMA_PERSIST_DIR=./chroma_db
